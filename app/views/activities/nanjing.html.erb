<link rel="stylesheet" href="http://cdn.bootcss.com/bootstrap/3.3.4/css/bootstrap.min.css">

<p id="notice"><%= notice %></p>

<%
l1 = @nanjings30.count
l15 = l1 - @nanjings30.where(lianxi:"无").count
l2 = @nanjings30.where.not(lianxi:"").count
l3 = @nanjings30.where(result: 0..100).count
l4 = @nanjings30.where(result: 1..100).count
l5 = @nanjings30.where(result: 2..100).count
#l6 = @nanjings30.where(result: 3..100).count
l7 = @nanjings30.where(result: 4..100).count

%>

<p>总数 <%= l1 %>条，有效数目 <%= l15 %>条，占<%= number_to_percentage(l15.to_f/l1*100, precision: 0) %>。</p>


<!--  <p>已收集信息 <%#= l2 %> / <%#= number_to_percentage(l2.to_f/l1*100, precision: 0) %></p>  -->
<p>已有联系方式并发送邀请<%= l3 %> / <%= number_to_percentage(l3.to_f/l15*100, precision: 0) %></p>
<p>通过认证<%= l4 %> / <%= number_to_percentage(l4.to_f/l15*100, precision: 0) %></p>
<p>已发名片<%= l5 %> / <%= number_to_percentage(l5.to_f/l15*100, precision: 0) %></p>
<!-- <p>已交互<%#= l6 %> / <%#= number_to_percentage(l6.to_f/l15*100, precision: 0) %></p> -->
<br \>
<!-- <p>群聊数目<%= l7 %> / <%= number_to_percentage(l7.to_f/l15*100, precision: 0) %></p> -->

    <table class="table">

      <thead>
        <tr>
          <th>出发日期</th>
          <th>出发城市</th>
          <th style="
    width: 400px;
">目的城市</th>
          <th>联系方式</th>   
          <th>进展(0已有联系方式,1已认证,2已发名片) </th>                
          <th>操作 </th>
          <th>Homepage</th>     

        </tr>
      </thead>

      <tbody>
      <% @nanjings30.each do |nanjing| %>
      <% if (nanjing.lianxi == "无")%>
        <tr class="danger">
        <% elsif (nanjing.result.nil?) %>
          <tr>
        <% else %>
          <tr class="success">
        <% end %>

          <td><%= nanjing.start_time %></td>
          <td><%= nanjing.start_city %></td>
          <td><%= nanjing.end_city %></td>
          <td><%= nanjing.lianxi %></td>
          <td><%= nanjing.result %></td>
          <td><%= link_to '修改', edit_activity_path(nanjing) %> <%= link_to '删除', nanjing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          <td><a href="<%= nanjing.f_homepage %>" target="_blank">点击查看</a></td>

        </tr>

      <% end %> 
    </tbody>

    </table>

